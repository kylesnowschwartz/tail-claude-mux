node_modules
dist
.turbo
*.log
.agents/skills/grill-me/
tree/
# Ephemeral agent session state
.aos/
# Praxis install backups
.ide_backup/
SPECKY-ROUTING.md

# Re-include Claude workflow scripts; the global gitignore ignores .claude/
# entirely, so un-ignore the dir, re-ignore its contents, then track only workflows/
!.claude/
.claude/*
!.claude/workflows/
