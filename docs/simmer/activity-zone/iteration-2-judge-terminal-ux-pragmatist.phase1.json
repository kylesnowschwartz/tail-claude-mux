{
  "iteration": 2,
  "phase": 1,
  "judge": "terminal-ux-pragmatist",
  "artifact_type": "single-file",
  "candidate_path": "docs/simmer/activity-zone/iteration-2-candidate.md",
  "lens_summary": "Real-world TUI usage during real working sessions. Behaviour across all four states (empty, single-source, multi-source, error-heavy) at narrow widths (~30 cols), under unfocus dimming, across a week of repeated use. Penalise sunny-mockup designs that hand-wave edges; reward calm peripheral-first designs that survive states. The 200ms-glance test governs.",
  "calibration_against_seed_and_iteration_1": {
    "seed_scores": { "hud-fidelity": 7, "four-questions": 8, "terminal-feasibility": 5, "composite": 6.7 },
    "iteration_1_phase2_scores": { "hud-fidelity": 9, "four-questions": 8, "terminal-feasibility": 8, "composite": 8.3 },
    "iteration_2_change_envelope": "Strictly surgical. The bones, edges, and four state mocks are preserved verbatim from iteration-1. Three buildability stalls fixed at the spec edges: (a) Rule 0 system-tag precedence in §Source position; (b) splitOutcome bridge clause in §Verb-glyph column rule 1; (c) explicit column-arithmetic walkthrough in §States preamble. One deferred-honestly future-work item (same-agent multi-thread chip collision) added to Open questions. No new glyphs, no design-surface changes, no new mocks.",
    "expected_movement": "hud-fidelity holds (no design-surface change, no chrome added, primitive ceiling preserved). four-questions holds (the stuck-vs-idle Pattern weakness from my iteration-1 phase-2 was scoped out of iteration-2 — the orchestrator explicitly framed iteration-2 as targeting the three buildability stalls only). terminal-feasibility lifts +1 because the three stalls land cleanly with one minor residual under-marking edge."
  },
  "stall_assessment": {
    "a_system_tag_precedence_under_multi_source": {
      "status": "closed",
      "evidence": "Lines 214–218 introduce Rule 0 as highest-precedence: when entry.source startsWith '[', render a 1-cell tone-coloured glyph at column 1, never as eyebrow, never as 3-char chip. Explicit reuse of existing severity-glyph vocab entries (no new glyph budget). Crucially, the rule states that system tags 'do not participate in the agent-source run-length stream' — a [bell] between two pi db92 rows leaves the run unbroken. Line 218 names the failure modes the rule prevents (a fresh eyebrow on [bell], or the nonsense chip '[b│ ...').",
      "multi_source_interleave_behaviour": "When [bell] arrives mid-multi-source, the chip stripe (cols 0–2) goes blank for that one row, the severity glyph occupies col 1, and pi/cc surrounding rows still chip-render because run-length-1 is preserved across the system-tag gap. The chip-column gap is itself diagnostic — it reads as 'a system event interrupted' the same way the verb-glyph blank-on-miss reads as 'non-tool event'. Self-consistent with §Verb-glyph column rule 4.",
      "lens_residual": "The rule is unambiguous in prose but not visualised. A user who only reads the four state mocks won't see a [bell]-mid-multi-source row. For week-of-use memorability, a 5-row mock fragment in the rules section would lock the visual in. Not score-affecting under this lens (the rule itself is clean); flagged as a candidate ASI for iteration-3 alongside the under-marking edge."
    },
    "b_splitOutcome_bridge_for_legacy_markers": {
      "status": "partially-closed",
      "evidence": "Lines 188–197 cite the function (apps/tui/src/index.tsx:210), quote the regex /^(.*?)(\\s*)(\\((passed|failed)\\))\\s*$/ verbatim, and specify the bridge: when tone === 'error' AND splitOutcome.outcome?.tone === 'error', strip the (failed) suffix and rely on column-1 SEV_ERROR (no double-mark). For (passed), keep the existing tone-coloured suffix as the sole success carrier (no glyph; no displacement).",
      "week_of_use_behaviour_passed_marker": "Preserved cleanly. Legacy producers that emit 'Tests passed (passed)' continue to render with the green-toned suffix exactly as today. The user's learned rule 'green (passed) tail = success' survives the redesign untouched. This is the right call: success is the unmarked default; suffix is its sole carrier; no col-1 chrome is spent on the common case.",
      "week_of_use_behaviour_failed_marker": "Preserved for the AND-conjunction case. Legacy producers that emit '(failed)' WITH tone:'error' get the new behaviour: stripped suffix + SEV_ERROR col-1 stripe. The user's learned rule 'red col-1 stripe = exception' lands cleanly in error-cascade windows.",
      "lens_residual_under_marking": "Rule 1 keys ONLY on tone === 'error' (line 186). A producer emitting (failed) with tone:'neutral' (the runtime's splitOutcome at index.tsx:215 already detects tone from the suffix alone, independent of entry.tone — verified by the realist) would fall through to Rule 2/3, render verb-glyph + tone-coloured (failed) suffix, and silently miss the SEV_ERROR stripe that State 4 promises. Week-of-use impact: the rule 'red col-1 = exception' becomes producer-config-dependent — a real working-session hazard. One-clause fix: broaden trigger to (tone === 'error') OR (splitOutcome.outcome?.tone === 'error'). The bridge solved over-marking but not under-marking."
    },
    "c_column_arithmetic_honesty_at_30_cols": {
      "status": "closed",
      "evidence": "Lines 296–304 explicitly state pad(1) | content(28) | pad(1), name that the leading space on most rows literally IS the left pad, and call out col-0 displacement when gutter (●) or chip head (pi│) appears. Resolves the iteration-1 ambiguity 'do mock rulers count chrome?'. The State-1 ruler reads 0–29 (30 cells) — verifiable.",
      "mock_walkthrough_30_cols": "State 2 row '●r build.ts' = 11 cells, fits with 19 cells slack. State 3 chip row 'pi│ r scenarios.ts' = 18 cells, fits with 12 cells slack. State 4 freshest+failed '●✗ src/index.tsx' = 16 cells, fits with 14 cells slack. Sparkline rows ' ▂▃▄▅▆▆▅▃' = 9 cells, fits with 21 cells slack. All four states honest at 30 cols.",
      "lens_residual_off_by_one": "The per-mode description budgets quoted in §Verb-glyph column ('≤24 cells at 30-col panel' for chip mode) and State 2 annotation ('≤27 cells' for eyebrow mode) imply description extends to col 29 (i.e. into the right pad). My computation against the explicit pad(1) | content(28) | pad(1) declaration says ≤23 (chip) and ≤26 (eyebrow). +1 off in both. The mocks themselves don't violate width — descriptions are pre-truncated and fit comfortably — but the truncation thresholds quoted in prose disagree with the panel-level rule by one cell. Cosmetic, not blocking. Realist's verified count was ≤22 / ≤25 (off-by-2 from iteration-1) — iteration-2 partially corrected toward the right number, partially away. A line that states the truncation budget exactly (≤23 / ≤26) would close it."
    }
  },
  "scores": {
    "hud-fidelity": {
      "score": 9,
      "rationale": "No design-surface change since iteration-1. Both terminal tests still pass cleanly: sparkline (8 cells × 8s/bucket, fixed ▁▂▃▄▅▆▇█ alphabet, ▁-floor as visible baseline, no '7/min' label re-stating the shape) and spellcheck (gutter cell, verb glyph, SEV_ERROR all on the line of work). ≤3 primitives per row holds across both eyebrow and chip modes; the new Rule 0 keeps system-tag rows at 2 primitives (system-tag glyph + description). Data-ink redundancy still closed. The freshest+failed double-encoding (●✗ at cols 0–1) and the chip's │ separator on every multi-source row remain soft demerits keeping this off 10 — both already flagged in iteration-1 phase-2; iteration-2 didn't touch them. The candidate's surgical scope was correct: hud-fidelity wasn't where the iteration-1 lift gap lived.",
      "passes_spellcheck_test": true,
      "passes_sparkline_test": true,
      "primitives_per_row_worst_case": 3,
      "data_ink_redundancy_closed": true
    },
    "four-questions": {
      "score": 8,
      "rationale": "Liveness, Exception, Latest answered cleanly by glance. Pattern partially answered: sparkline shape carries burst/decay/flat coherently for the active case (visible across the three active state mocks), but the window-empty case (ii) — flat ▁▁▁▁▁▁▁▁ — still renders identically whether the agent is wedged on a 60s+ Bash run or simply finished its burst. This is the lens-signature stuck-vs-idle gap that I flagged in iteration-1 phase-2 and proposed a Tier-3-dim-vs-Tier-2 fix for. Iteration-2 explicitly scoped to the three buildability stalls and did not address it; the open-questions list dropped it. Real working sessions hit this constantly (long compiles, slow tests, blocked subprocesses), and the user must context-switch to the agent's pane to disambiguate — the exact context-switch the activity zone exists to prevent. Holds at 8; not a regression, but the iteration-1-phase-2 weakness persists. This is my iteration-3 ASI.",
      "questions_answered_by_glance": ["liveness", "exception", "latest"],
      "questions_partially_answered": ["pattern"],
      "weakest_question": "pattern",
      "weakest_question_rationale": "Stuck-vs-idle conflation under window-empty sparkline persists from iteration-1: a wedged agent and a finished agent both render flat ▁▁▁▁▁▁▁▁. Cleavage requires either a sparkline-side Tier-step rule (Tier-3 dim for window-empty, Tier-2 for active-low) or an explicit cross-band reference to the rolodex severity spinner. Iteration-2 ships neither."
    },
    "terminal-feasibility": {
      "score": 9,
      "rationale": "Iteration-1's 8 was driven by the three buildability stalls explicitly named by the orchestrator. Iteration-2 closes (a) and (c) cleanly and (b) at 0.8 closure (over-marking solved, under-marking residual). The Rule 0 system-tag precedence is the cleanest of the three: branches first, reuses existing severity-glyph vocab (no glyph budget pressure), preserves source-run continuity across [bell], and self-consistent with the verb-glyph column-1 displacement mechanism — the rule fits the design's existing grain rather than adding a new layer. The column-arithmetic walkthrough makes the 30-col mocks auditable without re-counting; the +1 off-by-one in the per-mode description budgets is cosmetic. The splitOutcome bridge cites the function precisely, quotes the regex verbatim, handles the AND-conjunction case for double-mark prevention, and preserves the (passed) suffix as success's sole carrier (week-of-use behaviour for legacy success markers preserved cleanly). The under-marking residual (Rule 1 trigger keys only on tone:'error', not on splitOutcome.outcome.tone:'error') is a one-OR-clause fix and the right ASI candidate alongside stuck-vs-idle. Same-agent multi-thread chip collision is deferred-honestly with reasoning (forks across multiple design choices, benefits from user testing first) — acceptable for a surgical iteration. Holds short of 10 because of the under-marking residual and the deferred future-work item; both are realistic working-session edges that survive iteration-2.",
      "narrow_width_30_cols_drawn": true,
      "states_drawn": ["empty", "single-source", "multi-source", "error-heavy"],
      "states_handwaved": [],
      "state_intersections_undrawn": ["multi-source ∩ system-tag ([bell] mid-interleave — rule defined, mock omitted)", "multi-source ∩ error-heavy (chip-mode error cascade — rule defined, mock omitted)"],
      "unfocus_consistency_resolved": true,
      "new_glyphs_proposed_count": 6,
      "new_glyphs_pinned": true,
      "codepoint_width_risks_addressed": true,
      "verb_dictionary_closed": true,
      "buildable_today": true,
      "stalls_closed_count": 2.8,
      "stall_a_system_tag_precedence_closure": "1.0 (clean)",
      "stall_b_splitOutcome_bridge_closure": "0.8 (over-marking solved, under-marking residual)",
      "stall_c_column_arithmetic_closure": "1.0 (with cosmetic +1 in per-mode description budgets)"
    }
  },
  "lens_observations": {
    "two_hundred_ms_glance": "Holds for the four drawn states. Strengthened slightly under multi-source interleave because Rule 0 prevents [bell] from inducing a fresh eyebrow row mid-stream — the chip stripe stays stable, the eye keeps its anchor. Still fails for the window-empty stuck-vs-idle case: a flat sparkline reads the same whether the agent is wedged or done.",
    "multi_source_thrash_resolved": "Yes for distinct-agent run-length=1 with the iteration-1 hybrid rule. Now also resolved for system-tag interruptions: a [bell] in the middle of pi/cc interleaving doesn't break the run-length stream and doesn't waste a row on a one-time event. The chip stripe survives system-event peppering.",
    "demands_attention_check": "Sparkline calm. Gutter calm. Verb stripe calm. SEV_ERROR loud only on real exceptions. Chip column adds a 3-cell visual band on multi-source rows but it's structural, not salient. New system-tag glyph in col 1 borrows existing severity-bypass: it's loud only when the system event is loud (e.g. [bell] = waiting). No false-alarm primitive added.",
    "consistency_focus_unfocus": "Resolved in iteration-1; preserved in iteration-2. The per-primitive table is unambiguous; SEV_ERROR bypass + tier-slide for everything else produces the canonical 'dim landscape with bright red spots' HUD-at-rest signature. System-tag glyphs follow the same severity-bypass rule (tone-coloured, full intensity), which fits the unfocused-band pattern: the severity stripe is always bright whether or not you're looking.",
    "memorability_week_of_use": "Five verb glyphs are universal affordances (eye/list/magnify/pencil/play) — pre-learned. Closed dictionary + blank-on-miss means the column behaviour is stable across the week. The new system-tag rule reuses existing severity glyphs, so no new memorability load. Week-of-use rule for outcomes: 'green (passed) suffix = success, red col-1 stripe = exception' — clean for the AND-conjunction case but inconsistent for the under-marking edge (producer that emits (failed) with tone:'neutral' breaks the col-1 rule). The user learns the rule once and it works MOST of the time — the kind of intermittent inconsistency that erodes peripheral-trust over a week. This is the lens's most-cared-about residual.",
    "intersection_edges_remaining": "Three: (1) splitOutcome under-marking — Rule 1 trigger needs broadening to OR with splitOutcome.outcome?.tone === 'error'. One-line. Closes the week-of-use rule consistency. (2) Stuck-vs-idle Pattern conflation — sparkline window-empty Tier-3 dim vs active-low Tier-2 normal, plus an explicit cross-band reference noting that wedged-vs-finished is answered by the rolodex severity spinner above. Closes the four-questions Pattern weakness. (3) Same-agent multi-thread chip collision — deferred-honestly to future work; acceptable for surgical iteration but a real edge.",
    "stuck_vs_idle_pattern_failure": "Persists from iteration-1. The deliberation in my iteration-1 phase-2 lowered four-questions from 9 to 8 on this catch; iteration-2 chose to leave it un-fixed in favour of the three buildability stalls. The candidate's open-questions list also dropped it (only heading absorption / gutter gradient / same-agent multi-thread remain). For a single-focused iteration-3, this is the highest-leverage four-questions fix available.",
    "system_tag_mid_stream_visualisation": "Rule 0 is unambiguous in prose but not drawn. A real working session encounters [bell]-mid-multi-source weekly (every time an agent waits for input during parallel work). A 4–6 row mock fragment in the rules section would lock the visual in for week-of-use memorability. Not a score driver under this lens (the rule is clean); secondary candidate for iteration-3 if the ASI is split.",
    "column_arithmetic_off_by_one": "Cosmetic. The per-mode description budgets ('≤27' eyebrow, '≤24' chip) imply description extends into the right pad cell. My computation against pad(1)|content(28)|pad(1) yields ≤26 / ≤23. The mocks themselves are honest at 30 cols; the truncation thresholds in prose are slightly generous. A 1-line correction (≤23 / ≤26) would close it without disturbing anything else.",
    "micro_typography": "●r and ●✗ adjacency note still missing — same gap as iteration-1. Likely fine via nerd-font intra-glyph padding but undocumented. Soft demerit; not blocking."
  },
  "asi_candidate": {
    "title": "Close the four-questions Pattern weakness: stuck-vs-idle disambiguation via sparkline tier-step + explicit rolodex-spinner cross-reference",
    "summary": "A single focused refinement that lifts four-questions from 8 to 9 by closing the stuck-vs-idle Pattern conflation that has persisted across iteration-1 and iteration-2. Two coordinated edits in §Sparkline contract — both spec-only, no new chrome, no new glyphs, no mock changes. The fix is the lens's signature catch (long-running tools = the working-session reality TUI users face daily) and is the highest-leverage four-questions improvement available. The under-marking edge from stall (b) is the secondary candidate; targeted by the realist; deferred here because the pragmatist's distinctive contribution is the working-session glance failure, not the producer-config edge.",
    "concrete_changes": [
      "Patch §Sparkline contract empty-states row case (ii) window-empty: render `▁▁▁▁▁▁▁▁` at **Tier 3 dim** (vs Tier 2 for active-but-low-rate). The sparkline has THREE visual modes now: (i) suppressed (no logs ever); (ii) Tier-3 dim flat (logs exist but none in 64s — quiet); (iii) Tier-2 normal active (something in the 64s window). Cleaves 'low-rate active' from 'window-empty quiet' by glance — the user reads bright-flat as 'agent doing slow work' and dim-flat as 'agent quiet'. Free: no glyph budget, no new primitive, just a tier-class change in the existing render path. This is the single most lens-critical refinement.",
      "Add one paragraph to §Sparkline contract (or a new §Cross-band coherence subsection) clarifying the architectural division of labour: the sparkline answers liveness-of-log-stream (have entries arrived in the 64s window?). Liveness-of-current-task (is the agent wedged on a long tool call vs finished and idle?) is answered by the rolodex severity glyph IMMEDIATELY ABOVE the activity zone — working spinner = wedged, ready/stopped = finished. The two signals compose: spinner + dim-flat = 'wedged on a long tool call' (e.g. 90-second test suite); ready + dim-flat = 'finished and idle'. This makes the activity zone honest about what it CAN'T answer alone, and locks the user's mental model: stuck-vs-idle is a two-band glance, not a one-band failure.",
      "Optional micro-touch: add stuck-vs-idle to the open-questions list as 'closed in iteration-3' so the trajectory of the design's four-questions Pattern weakness is legible to readers (was open in seed, deferred in iteration-2, closed in iteration-3 via the two-band cross-reference)."
    ],
    "why_this_lens": "The Terminal-UX Pragmatist grades on real working-session behaviour during a week of repeated use, not single-state mockups. The stuck-vs-idle Pattern failure is THE canonical lens edge: every TUI user encounters long-running tools (compiles, tests, slow Bash, blocked subprocesses) and asks 'is this agent wedged or done?' multiple times per day. Iteration-1 surfaced it; iteration-2 chose buildability over it; iteration-3 should close it because it's the only remaining gap on the lens's signature criterion. The fix is a tier-class change plus one paragraph of architectural clarification — the lightest possible intervention for the largest remaining lens-relevant lift. The splitOutcome under-marking edge is real but is more naturally the realist's catch (codebase-verified, one-line code-path fix, one specific producer config); deliberation will surface it in phase-2 regardless. The pragmatist's distinctive contribution should be the working-session glance failure that survives across both prior iterations.",
    "expected_score_lift": {
      "hud-fidelity": "+0 (already 9; tier-class change in sparkline render adds zero chrome; cross-band reference is prose, not a primitive)",
      "four-questions": "+1 (8 → 9; stuck-vs-idle is the only Pattern weakness remaining; closing it answers 'cycling, converging, stuck?' fully by glance via the two-band composition)",
      "terminal-feasibility": "+0 to +1 (already 9; if the under-marking edge is also closed as a secondary patch in the same iteration, +1 to 10 — but the ASI is single-focused on stuck-vs-idle)"
    },
    "single_focused": true,
    "scope_notes": "Spec-only; no source code changes, no vocab.ts entries, no new glyphs, no new mocks, no producer dependency. The Tier-3-dim render is a class change inside the existing sparkline path. The cross-band paragraph is documentation. Total candidate edit: ~6 lines of prose + one Tier annotation in the sparkline contract table.",
    "secondary_residual_left_for_iteration_n": "splitOutcome under-marking (broaden Rule 1 trigger to OR with splitOutcome.outcome?.tone === 'error') — naturally caught by the realist; one-clause fix; closes the (b) stall residual. If iteration-3 has bandwidth for two patches, this is the second.",
    "tertiary_residual_left_for_iteration_n": "Same-agent multi-thread chip collision (open-question 3) — deferred to user testing per iteration-2's reasoning; not a single-clause fix, requires design forking; honest deferral."
  }
}
